name: Stage 3.72 documentation closure applicator

on:
  push:
    branches:
      - docs/stage-03-72-runtime-closure

permissions:
  contents: write

jobs:
  apply:
    if: github.event.head_commit.message == 'chore: stage 3.72 documentation closure applicator'
    runs-on: ubuntu-latest
    steps:
      - name: Check out closure branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Synchronize Stage 3.72 lifecycle documentation
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          merge_sha = "e935f19b68624f0b8be6ca293ead8f4395cf555e"
          runtime_head = "b6cd7766a6a584440dfdd7d00c35c34c8553a9ff"
          planning_merge = "2144c52f4dc5ba9e917947d396ced1f3c572fe51"
          ci_run = "34091588982"

          def replace_once(path: str, old: str, new: str):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one anchor, found {count}: {old!r}")
              p.write_text(text.replace(old, new, 1))

          # ROADMAP
          roadmap = Path("docs/ROADMAP.md")
          text = roadmap.read_text()
          text = text.replace("| Version | 1.1.91 |", "| Version | 1.1.92 |", 1)
          text = text.replace("| Last Review Date | 2026-09-06 |", "| Last Review Date | 2026-09-07 |", 1)
          text = text.replace(
              "| Next Review Date | Before Stage 3.72 implementation or any later Portfolio Position / Cost Basis scope, Stage 3.25 evidence-collection plan review, evidence collection, formal Security Review, ADR-008 acceptance, provider proposal, Feature 3D corporate-actions source/use planning, privacy-lifecycle migration proposal, or the next separately reviewed audit-remediation scope |",
              "| Next Review Date | Before any later Portfolio Position / Cost Basis scope, Stage 3.25 evidence-collection plan review, evidence collection, formal Security Review, ADR-008 acceptance, provider proposal, Feature 3D corporate-actions source/use planning, privacy-lifecycle migration proposal, or the next separately reviewed audit-remediation scope |",
              1,
          )
          old_line = next((line for line in text.splitlines() if line.startswith("| 3.72 — Portfolio Position Projection / Honest Market-Unavailable Semantics planning |")), None)
          if old_line is None:
              raise SystemExit("ROADMAP Stage 3.72 planning row not found")
          new_rows = "\n".join([
              f"| 3.72 — Portfolio Position Projection / Honest Market-Unavailable Semantics planning | Freeze the additive public positions read-model/API/UI contract over canonical Stage 3.71 quantity/WAC/acquisition basis, deterministic BusinessDate/as-of behavior, acquisition-basis allocation, and explicit non-fabricated market-unavailable semantics without changing ADR-009 or activating a provider | Complete / canonical through PR #142 squash merge `{planning_merge}`; planning authority activated without itself authorizing runtime implementation |",
              f"| 3.72 — Portfolio Position Projection / Cost Basis View runtime | Expose deterministic open STOCK/BOND positions from the canonical Stage 3.71 ledger/WAC engine through a dedicated API and Web projection, including acquisition-basis allocation and explicit unavailable market valuation | Complete / PR #145 squash-merged into protected `develop` at `{merge_sha}` from exact runtime head `{runtime_head}`; CI #413 / run `{ci_run}` 10/10 SUCCESS; reviewed Stage 3.72 runtime scope complete |",
              "| 3.72 — Portfolio Position Projection lifecycle/documentation closure | Synchronize active canonical documentation with the already-merged Stage 3.72 runtime; record purpose, implementation, expected behavior, verification, preserved boundaries and residual scope without runtime change | Closure candidate on this documentation branch; canonical only after protected merge of the exact closure tree |",
          ])
          text = text.replace(old_line, new_rows, 1)
          roadmap.write_text(text)

          # SOURCE OF TRUTH
          sot = Path("docs/SOURCE_OF_TRUTH.md")
          text = sot.read_text()
          text = text.replace("| Version | 1.4.93 |", "| Version | 1.4.94 |", 1)
          text = text.replace("| Last Review Date | 2026-09-06 |", "| Last Review Date | 2026-09-07 |", 1)
          text = text.replace(
              "| Next Review Date | Before Stage 3.72 implementation or any later Portfolio Position / Cost Basis scope, Stage 3.25 privacy evidence-collection plan review, Feature 3D corporate-actions source/use planning, or the next separately reviewed audit-remediation scope |",
              "| Next Review Date | Before any later Portfolio Position / Cost Basis scope, Stage 3.25 privacy evidence-collection plan review, Feature 3D corporate-actions source/use planning, or the next separately reviewed audit-remediation scope |",
              1,
          )
          lines = text.splitlines()
          for i, line in enumerate(lines):
              if line.startswith("**Last completed implementation stage:"):
                  lines[i] = f"**Last completed implementation stage: Stage 3.72 — Portfolio Position Projection / Cost Basis View / PR #145 squash merge `{merge_sha}` / protected tree `3775b533867c2fb6de473da84a105cf3367cf848` / CI #413 run `{ci_run}` 10/10 SUCCESS**"
              if line.startswith("**Current Portfolio Position / Cost Basis lifecycle:"):
                  lines[i] = (
                      f"**Current Portfolio Position / Cost Basis lifecycle: Stage 3.70 / ADR-009 is accepted and canonical through PR #137 squash merge `b772e52221fbb694b3116bd1b579db99d4e56302`; "
                      f"Stage 3.71 runtime is canonical through PR #138 squash merge `827f49f909ace5a3f7bcb2a3f51ce7c638c458ad` and lifecycle/documentation closure through PR #140 squash merge `97c35a7b0fe7e2cd487c63d03b8ba79f80dcfc7b`. "
                      f"Stage 3.72 planning is canonical through PR #142 squash merge `{planning_merge}`. Stage 3.72 runtime is canonical through PR #145 squash merge `{merge_sha}` from exact head `{runtime_head}` after CI #413 / run `{ci_run}` 10/10 SUCCESS. "
                      "The shipped Stage 3.72 projection exposes only open STOCK/BOND positions derived from the canonical Stage 3.71 ledger/WAC engine: quantity, authoritative weighted-average cost, remaining acquisition basis, total acquisition basis, and independent Half-Even acquisition-basis weights. The dedicated positions endpoint uses deterministic ticker ordering, optional endpoint-local BusinessDate cutoff semantics, ownership isolation, repeatable-read replay and fail-closed unsupported correction/reversal handling. "
                      "Market valuation is intentionally explicit as `UNAVAILABLE / NO_APPROVED_MARKET_PRICE_SOURCE`; market price, market value, unrealized gain/loss, market weight, provider and market as-of remain null and are not inferred. Closed positions are omitted; an inactive catalog lifecycle does not hide an owned open position; existing `PortfolioSummary.positions` behavior and `analytics.snapshot_positions` remain unchanged. "
                      "No market provider activation, production rollout, XIRR/returns, imported SELL expansion, correction/reversal runtime, tax basis, bond NKD, notifications, AI, Redis/workers or later-stage scope is authorized by Stage 3.72. Stage 3.72 runtime is complete; this documentation closure synchronizes the active canonical surfaces without runtime change.**"
                  )
          sot.write_text("\n".join(lines) + "\n")

          # DOCUMENT INDEX
          index = Path("docs/DOCUMENT_INDEX.md")
          text = index.read_text()
          text = text.replace("| Version | 1.1.69 |", "| Version | 1.1.70 |", 1)
          text = text.replace("| Last Review Date | 2026-09-06 |", "| Last Review Date | 2026-09-07 |", 1)
          text = text.replace(
              "| Next Review Date | Before Stage 3.72 implementation or any later Portfolio Position / Cost Basis scope, Stage 3.25 evidence-collection plan review, evidence collection, formal Security Review, ADR-008 acceptance, provider proposal, Feature 3D corporate-actions source/use planning, privacy-lifecycle migration proposal, or the next separately reviewed audit-remediation scope |",
              "| Next Review Date | Before any later Portfolio Position / Cost Basis scope, Stage 3.25 evidence-collection plan review, evidence collection, formal Security Review, ADR-008 acceptance, provider proposal, Feature 3D corporate-actions source/use planning, privacy-lifecycle migration proposal, or the next separately reviewed audit-remediation scope |",
              1,
          )
          text = text.replace("| SOT-001 | Source of Truth | 1.4.93 | Approved |", "| SOT-001 | Source of Truth | 1.4.94 | Approved |", 1)
          lines = text.splitlines()
          insert_at = None
          for i, line in enumerate(lines):
              if "Stage 3.72" in line and "STAGE_03_72_PORTFOLIO_POSITION_PROJECTION_PLANNING.md" in line:
                  lines[i] = f"| Stage 3.72 Portfolio Position Projection / Honest Market-Unavailable Semantics planning | Complete/canonical through PR #142 squash merge `{planning_merge}`; planning authority activated without provider/runtime activation by the planning merge itself | `stages/STAGE_03_72_PORTFOLIO_POSITION_PROJECTION_PLANNING.md` |"
                  insert_at = i + 1
                  break
          if insert_at is None:
              raise SystemExit("DOCUMENT_INDEX Stage 3.72 planning entry not found")
          rows = [
              f"| Stage 3.72 Portfolio Position Projection / Cost Basis View runtime | Complete/canonical through PR #145 squash merge `{merge_sha}` from exact runtime head `{runtime_head}` after CI #413 / run `{ci_run}` 10/10 SUCCESS | `stages/STAGE_03_72_PORTFOLIO_POSITION_PROJECTION_IMPLEMENTATION_CLOSURE.md` |",
              "| Stage 3.72 lifecycle/documentation closure | Records purpose, implemented behavior, expected user-visible outcome, verification, preserved architecture boundaries and residual non-Stage-3.72 scope; becomes canonical only after protected merge of this documentation synchronization | `stages/STAGE_03_72_PORTFOLIO_POSITION_PROJECTION_IMPLEMENTATION_CLOSURE.md` |",
          ]
          lines[insert_at:insert_at] = rows
          index.write_text("\n".join(lines) + "\n")

          closure = f'''# Stage 3.72 — Portfolio Position Projection / Cost Basis View implementation closure

| Field | Value |
| --- | --- |
| Document ID | STAGE-03-72-PORTFOLIO-POSITION-PROJECTION-CLOSURE |
| Version | 1.0.0-candidate |
| Status | RUNTIME COMPLETE / DOCUMENTATION CLOSURE CANDIDATE — CANONICAL AFTER PROTECTED MERGE |
| Owner | Principal Architect |
| Runtime base | `develop@{planning_merge}` |
| Runtime PR | `#145` |
| Runtime exact head | `{runtime_head}` |
| Runtime squash merge | `{merge_sha}` |
| Runtime protected tree | `3775b533867c2fb6de473da84a105cf3367cf848` |
| Exact-head CI | `#413` / run `{ci_run}` / 10 of 10 required contexts SUCCESS |
| Review state | Stage 3.72 runtime review declared complete by the Principal Architect before this documentation closure publication |
| Date | 2026-09-07 |

## 1. Why Stage 3.72 exists

Stage 3.71 made portfolio position math authoritative, deterministic and financially safe, but it deliberately stopped short of a public position projection. A user could record BUY/SELL transactions and the backend could rebuild quantity, Weighted Average Cost and remaining acquisition basis, yet the product still lacked an honest read model that exposed those facts directly.

Stage 3.72 closes that product gap without pretending that approved live market data exists. Its purpose is to let the user answer four cost-basis questions from the immutable ledger itself:

1. what open instruments are currently owned;
2. how many units are open;
3. what the authoritative average acquisition price is;
4. how much acquisition basis remains and what share of total acquisition basis that position represents.

Market valuation is not required to answer those questions. Therefore Stage 3.72 deliberately exposes market valuation as unavailable instead of inventing prices, values, profit/loss or market weights.

## 2. What was implemented

### 2.1 Canonical backend projection

A dedicated portfolio-position projection was added on top of the Stage 3.71 engine. The implementation reuses canonical `position.Apply`; it does not introduce a second WAC or cost-basis calculator.

The PostgreSQL replay seam reconstructs position state from accepted BUY/SELL ledger rows in deterministic order and returns open STOCK/BOND positions with:

- `ticker`;
- `assetType`;
- exact Decimal quantity;
- authoritative Weighted Average Cost;
- remaining acquisition basis;
- independent acquisition-basis weight;
- explicit unavailable market-valuation state.

The aggregate denominator is the sum of acquisition basis for all open STOCK/BOND positions. Each weight is independently calculated with scale-8 Half-Even rounding. Final rounded weights are not force-normalized. If open positions exist but total acquisition basis is exactly `0.00000000`, the position weight is `null` rather than fabricated as zero.

Closed positions are omitted. Asset catalog lifecycle status does not erase an owned open position from the projection.

### 2.2 Deterministic as-of semantics

`GET /api/v1/portfolios/{{portfolioId}}/positions` accepts an endpoint-local optional `asOfDate`.

When supplied, only position-affecting ledger rows with `tradeDate <= asOfDate` are replayed, and `inputsAsOf` is the requested BusinessDate even when no qualifying rows exist.

When omitted, all accepted position-affecting rows are replayed, including accepted future-dated trades, and `inputsAsOf` is the maximum included trade date. An empty position ledger returns `items=[]`, zero RUB total acquisition basis and `inputsAsOf=null`.

Unsupported correction/reversal state fails closed. The endpoint does not reuse the older shared OpenAPI `AsOfDate` parameter whose default semantics refer to the latest completed MOEX business date.

### 2.3 Ownership and consistency

The PostgreSQL read executes inside one read-only Repeatable Read transaction. Portfolio ownership/subject isolation is checked before ledger replay in that same transaction. This preserves anti-enumeration semantics and prevents a projection from mixing authorization and financial reads across inconsistent snapshots.

Stable public ordering is `ticker ASC`, with an internal deterministic tie-breaker where needed.

### 2.4 Honest market-unavailable contract

Every Stage 3.72 position returns:

```text
status = UNAVAILABLE
reason = NO_APPROVED_MARKET_PRICE_SOURCE
marketPrice = null
marketValue = null
unrealizedGain = null
marketWeight = null
provider = null
asOf = null
```

No provider lookup occurs. No frontend or backend code derives market values from transaction prices or acquisition basis.

### 2.5 OpenAPI and HTTP boundary

The implementation adds:

```text
GET /api/v1/portfolios/{{portfolioId}}/positions
```

with `getPortfolioPositions` as the operation ID and responses for:

- `200` success;
- `400` invalid endpoint-local `asOfDate`;
- `401` unauthenticated request;
- `404` inaccessible/not-found portfolio with existing anti-enumeration behavior.

The contract adds explicit Stage 3.72 position, calculation and market-unavailable schemas plus a financially consistent example. `acquisitionBasisWeight` is constrained to `[0,1] | null`.

### 2.6 Next.js product surface

The portfolio detail view now loads portfolio metadata, summary, the dedicated Stage 3.72 positions projection and transactions together.

The positions UI provides:

- explicit loading state;
- positions-specific error state;
- explicit empty state;
- desktop table;
- mobile cards;
- quantity;
- average acquisition price;
- acquisition basis;
- acquisition-basis weight;
- visible `Market valuation unavailable` state.

The UI never reconstructs positions from summary or transaction rows when the positions request fails.

A dedicated quantity formatter preserves fractional position precision instead of rounding tiny valid quantities to `0.00`. Ratio-to-percent conversion is presentation-only and does not recalculate allocation.

The stale-load guard binds the result to request generation, access token, authenticated principal identity and portfolio identity before committing state. Accepted transaction/import mutations reload the positions projection.

## 3. Expected user-visible behavior

For the canonical example:

```text
BUY 100 @250
BUY 100 @300
SELL 50
```

Stage 3.72 must expose:

```text
SBER
Quantity               150
Average price           275 RUB
Acquisition basis    41,250 RUB
Acquisition-basis weight   derived from total open acquisition basis

Market valuation unavailable
```

The user receives a real cost-basis view based only on accepted ledger facts. They do not receive invented market price, market value, unrealized P/L or market allocation.

For the fractional witness, authoritative state remains:

```text
quantity = 0.20000000
WAC = 275.12345678
acquisitionBasis = 55.02469136
```

The implementation never re-derives WAC as `basis / quantity`.

## 4. Why this architecture was chosen

The Stage 3.72 projection is deliberately additive rather than a rewrite of Stage 3.71.

This preserves four important boundaries:

1. **one financial engine** — WAC/acquisition basis remain owned by Stage 3.71 `position.Apply`;
2. **honest data semantics** — missing approved market data is represented as unavailable, not approximated;
3. **API compatibility** — the older `PortfolioSummary.positions` contract remains untouched;
4. **future extensibility** — a later separately reviewed market-data stage can replace the unavailable market projection without changing ledger-derived cost basis.

A provider was not activated merely to make the screen look richer because that would combine source-rights, provenance, freshness, outage, currency and valuation-methodology decisions with a stage whose approved responsibility is ledger-derived position projection.

## 5. Verification and review evidence

Runtime PR `#145` was published from exact head:

```text
{runtime_head}
```

GitHub Actions CI `#413` / run `{ci_run}` completed with all 10 required protected contexts successful:

- PostgreSQL migration validation;
- Go tests, including PostgreSQL-backed Stage 3.72 integration/regression coverage;
- Go race tests;
- Go vet;
- Go vulnerability scan;
- dependency security scan;
- frontend typecheck, tests and production build;
- OpenAPI contract/example validation;
- Python tests;
- Docker Compose validation.

The runtime PR was mergeable and squash-merged into protected `develop` as:

```text
{merge_sha}
```

The resulting protected tree is:

```text
3775b533867c2fb6de473da84a105cf3367cf848
```

The Principal Architect subsequently declared the Stage 3.72 runtime review complete and authorized its canonical documentation synchronization.

## 6. Preserved boundaries and non-goals

Stage 3.72 does **not** authorize or implement:

- approved/live market-price provider activation;
- market value or unrealized P/L;
- market-weight allocation;
- XIRR/return expansion;
- imported SELL expansion;
- correction/reversal runtime;
- FIFO/tax lots or tax basis;
- bond accrued interest/NKD;
- Corporate Actions Feature 3D source activation;
- notifications or AI;
- Redis, worker or microservice expansion;
- production provider rollout.

`analytics.snapshot_positions` is untouched. Existing Stage 3.71 snapshot totals and legacy `PortfolioSummary.positions` semantics are intentionally preserved.

## 7. Residual risk and next decision boundary

The main visible limitation is intentional: market valuation remains unavailable until an approved source/use/provenance/freshness design is separately reviewed and authorized.

That limitation is preferable to fabricated or weakly sourced market data. Stage 3.72 is complete when users can reliably inspect their ledger-derived open positions and acquisition basis while the system explicitly states that market valuation is unavailable.

Any later step that introduces a market provider, valuation, unrealized P/L, market weights, returns, tax basis or broader position semantics is a new separately reviewed stage and must not be inferred from this closure.

## 8. Closure statement

Stage 3.72 runtime implementation is complete and canonical through PR #145 squash merge `{merge_sha}`. The implementation delivers the intended Portfolio Position Projection / Cost Basis View over the canonical Stage 3.71 engine, with deterministic replay, endpoint-local as-of behavior, acquisition-basis allocation, ownership isolation, Web presentation, OpenAPI contract and explicit honest market-unavailable semantics.

This documentation-only closure changes no runtime, financial calculation, API behavior, schema, migration, provider configuration or production deployment. It becomes the canonical Stage 3.72 lifecycle record only after this exact documentation synchronization is reviewed, passes required CI, and is protected-merged into `develop`.
'''
          Path("docs/stages/STAGE_03_72_PORTFOLIO_POSITION_PROJECTION_IMPLEMENTATION_CLOSURE.md").write_text(closure)
          PY

          rm -f .github/workflows/stage372-doc-closure.yml
          git diff --check

      - name: Commit and publish documentation closure
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git status --short
          git commit -m "docs: close Stage 3.72 portfolio position projection lifecycle"
          git push origin "HEAD:${GITHUB_REF_NAME}"
