# Stage 3.41 — P3-09 Next.js Security Maintenance Implementation

| Field | Value |
| --- | --- |
| Status | PUBLISHED IMPLEMENTATION — publication history recorded; P3-09 closure not claimed |
| Date | 2026-08-29 |
| Original finding | P3-09 — Next.js maintenance |
| Canonical base | `develop@559b57d0951cdc67125c2f72fc1fcfb34399e90e` |
| Implementation branch | `fix/stage-03-41-p3-09-nextjs-16-3-3` |
| Planning artifact | Stage 3.40; exact approved plan blob `33cb076fa529d3efcdfbea9a95d111aec30ccbad` |
| Frozen target | `next 16.3.2 -> 16.3.3` |
| package.json candidate blob | `d6d605620e1bff426998d8bda716b7c2eda0613d` |
| pnpm-lock.yaml candidate blob | `b3d656e792bdd28b16dea553b378f15f553b3074` |
| package.json SHA256 | `d55dab239a4252e6d536923492c4645bb78c7cfcc065b3bf4546692b8aabe86c` |
| pnpm-lock.yaml SHA256 | `0b8d8e676bc848a422c730114bd0aa48bc2bb16a154bda2a5adcff54efc0f3c5` |
| Internal Review Evidence | WITHHELD — external published-head phase pending |
| P3-09 lifecycle | OPEN |

## 1. Problem

OpenInvest Web remained pinned to Next.js `16.3.2`, while the maintained 16.x line released
`16.3.3` with fixes for two Critical upstream advisories. The dependency version therefore remained
inside the affected ranges until this candidate.

## 2. Root cause

The project intentionally uses an exact framework pin. Exact pinning avoids silent framework drift but
makes security maintenance an explicit governed operation. The patched upstream version appeared after
the prior dependency baseline was frozen.

## 3. Failure / attack scenarios

### 3.1 Windows-hosted-server advisory

Upstream marks `>=16.0 <16.3.3` as affected. Repository evidence does not demonstrate Windows
production hosting, so dependency applicability is recorded without claiming proven OpenInvest
exploitability.

### 3.2 AVIF Image Optimization advisory

Upstream marks the affected 16.x range below `16.3.3`. Repository review found no current
`next/image` usage and no AVIF opt-in in `next.config.ts`; therefore an active OpenInvest path is
not asserted.

### 3.3 Maintenance stagnation

Keeping `16.3.2` indefinitely merely because project-specific exploitability is unproven would retain
a known-affected dependency and increase later emergency-response cost.

## 4. Impact

The original repository-audit severity remains P3. Upstream vulnerability severity is Critical, but
project-specific reachability was not demonstrated. The practical project risk is known affected-version
state plus future topology/configuration applicability and avoidable maintenance debt.

## 5. Chosen remediation

- `next 16.3.2 -> 16.3.3`;
- keep React/ReactDOM `19.2.7`;
- keep TypeScript, @types, Node policy and `pnpm@11.8.0`;
- let the real pnpm resolver regenerate Next.js-related lock identities;
- accept only the exact expected Next.js-family lockfile transformation;
- make no application-source or architecture changes.

## 6. Rejected / non-selected approaches

### Stay on 16.3.2

Rejected as a long-lived remediation because the installed version remains in the upstream affected
range.

### Move to a 16.4 canary for the cache-components memory report

Rejected. OpenInvest does not currently enable `cacheComponents` / `use cache`; applicability is not
evidenced and canary adoption would broaden risk.

### Bundle React maintenance

Rejected as unnecessary scope. React and ReactDOM `19.2.7` remain compatible with the target.

### Hand-author lockfile identities

Rejected. Runtime dependency state must be generated by the real resolver and verified against the
frozen expected transformation.

## 7. Local tooling forensic history

### Runner v1 — false drift result after failed preflight

**Cause:** a preflight helper treated the direct dependency line `specifier: 16.3.2` as a non-Next
occurrence.

**Secondary cause:** v1 lacked immediate shell termination after that helper error. Its expected oracle
was not created, but a later comparison still ran and emitted a misleading drift failure.

**Impact:** local-only execution anomaly. No commit, push, PR or protected-branch mutation occurred.

**Remediation:** v2 added fail-fast shell behavior and a structural comparator.

### Runner v2 — structural comparator false positive

The uploaded v2 evidence was reviewed before v3 was created.

**Observed raw result:** pnpm changed only the exact approved Next.js-family patch identities. No
unrelated transitive dependency changed. Resolver-generated package.json and lockfile from v1 and v2
were byte-identical.

**Cause:** v2 normalized snapshot package headers ending with `:`, but the pnpm snapshot for
`@next/env` is an inline empty mapping:

`'@next/env@16.3.2': {}`

The parser therefore normalized all SWC snapshot headers but missed that one `@next/env` line.

**Failure scenario:** the comparator reported semantic drift although the normalized-before and
normalized-after artifacts differed by only the missed `@next/env` patch-version line.

**Impact:** execution stopped safely; no repository mutation was published. The false positive cost
review time but did not broaden implementation scope.

**Revised solution:** v3 removes structural YAML-line inference. On the exact frozen base it constructs
the exact expected lockfile by replacing 31 known Next.js-family version occurrences and ten known npm
integrity values, then requires the real pnpm resolver result to byte-match it.

**Why chosen:** this is simpler, deterministic, auditable, and fail-closed for the exact frozen
implementation base.

### Runner v3 — final scope-accounting false positive

Runner v3 completed the substantive dependency/runtime verification before its final
scope-accounting assertion:

1. exact expected frozen-base resolver transformation comparison — PASS;
2. manifest invariants — PASS;
3. `pnpm install --frozen-lockfile` — PASS;
4. `pnpm run typecheck` — PASS;
5. `pnpm test` — PASS;
6. `pnpm run build` — PASS;
7. `pnpm audit` — PASS;
8. `git diff --check` — PASS.

After those checks, runner v3 executed its final changed-file assertion using
`git diff --name-only`. The new Stage 3.41 dossier was still untracked, and that Git command does not
list untracked files. The assertion therefore observed only:

- `frontend-next/package.json`;
- `frontend-next/pnpm-lock.yaml`.

It did not observe
`docs/stages/STAGE_03_41_NEXTJS_SECURITY_MAINTENANCE_IMPLEMENTATION.md`, reported
`FAIL: final changed-file scope mismatch`, and exited with status `71`.

**Classification:** tooling/evidence scope-accounting defect only. This was not an OpenInvest runtime,
dependency, application-security, authentication, financial-logic, or supply-chain defect.

**Subsequent verification:** after the v3 exit, the substantive commands were manually repeated against
the same local dependency candidate:

- `pnpm install --frozen-lockfile` — PASS;
- `pnpm run typecheck` — PASS;
- `pnpm test` — PASS (`44` standard tests and `2` component tests; `0` failures);
- `pnpm run build` — PASS on Next.js `16.3.3`;
- `pnpm audit` — PASS (`No known vulnerabilities found`);
- `git diff --check` — PASS.

The v3-generated evidence package preserves the v3 runner execution through its status-71
scope-accounting failure. The later successful manual rerun is separate evidence preserved in the
complete terminal transcript; it is not claimed to be contained inside the earlier v3 evidence package.

## 8. Implementation result

Changed runtime/dependency files:

1. `frontend-next/package.json`
2. `frontend-next/pnpm-lock.yaml`

The direct Next.js pin is exactly `16.3.3`; React and ReactDOM remain exactly `19.2.7`.

The actual resolver output byte-matched the exact frozen-base expected transformation. No unrelated
dependency drift was accepted.

## 9. Local regression / deterministic evidence

Verified execution environment recorded in the terminal evidence:

- Node `v22.22.2`;
- pnpm `11.8.0`.

Runner v3 substantive checks recorded as PASS before its later tooling-only status-71 exit:

1. exact package.json comparison;
2. exact expected lockfile byte comparison;
3. frozen manifest invariants;
4. `pnpm install --frozen-lockfile`;
5. `pnpm run typecheck`;
6. `pnpm test`;
7. `pnpm run build`;
8. `pnpm audit`;
9. `git diff --check`.

After the runner v3 scope-accounting failure, the terminal transcript separately records a successful
manual rerun of:

1. `pnpm install --frozen-lockfile`;
2. `pnpm run typecheck`;
3. `pnpm test`;
4. `pnpm run build`;
5. `pnpm audit`;
6. `git diff --check`.

Evidence boundaries are explicit:

- the v3 evidence package covers the v3 execution through its final tooling-only failure;
- the complete terminal transcript covers both the v3 execution and the subsequent manual rerun.

The supplied terminal transcript does not independently evidence `NEXT_TELEMETRY_DISABLED=1`, so this
dossier does not claim that environment setting as verified execution evidence.

## 10. Review history

Stage 3.40 planning completed its exact-published-head review and planning merge. That approval did not
authorize this implementation.

Internal review evidence remains intentionally withheld from the repository and Draft PR evidence
surface until the External published-head verdict, as required by `docs/REVIEW_WORKFLOW.md` v1.3.0.

The complete Internal review chronology, including findings and remediations, remains preserved in the
designated review chat and local review evidence. It will be published in the required evidence-only
follow-up after the External verdict and then verified for exactness and absence of semantic/runtime
drift.

Repository-visible Internal Review Evidence status:
`WITHHELD — external published-head phase pending`.

This withholding does not claim that Internal review history did not occur and does not erase or
rewrite any Internal review event. It is a publication-timing control only.

### External published-head review attempt 1

External review was performed independently against Draft PR #101 at exact published implementation
head `5ea1f7f29cf0ab9225460e01076255f32e2cf4cf` after GitHub CI #289 / run `33274458971`
completed `10/10 SUCCESS`.

Result: `REQUEST CHANGES`.

Finding: `EXT-STAGE-03-41-P3-01` — P3, documentation/governance evidence integrity only.

Problem: the published dossier still carried pre-commit lifecycle wording after the implementation
commit, Draft PR publication, and exact-head CI had already occurred.

Root cause: the approved pre-publication dossier bytes were committed unchanged, so temporary
pre-commit status text became stale on the published head.

Failure scenario: a later reviewer could reconstruct the repository as already committed with a Draft
PR and successful CI while the canonical dossier simultaneously described those completed gates as
future work.

Project impact: governance/evidence integrity only. External review found no Next.js runtime defect,
dependency-resolution defect, application-security defect, authentication/session issue,
financial-logic issue, API/database change, or unrelated supply-chain drift.

Remediation: this dossier revision replaces stale active pre-commit wording with publication-current
state, separates completed publication gates from genuinely remaining gates, and preserves P3-09 as
OPEN. Runtime/dependency bytes are unchanged.

Required regression evidence: required GitHub CI on the revised exact head followed by a fresh
exact-published-head External re-review.

Resolution criteria for `EXT-STAGE-03-41-P3-01`: the docs-only remediation must be published on a
new exact head, required GitHub CI must pass on that head, and a fresh exact-published-head External
re-review must confirm that the finding is resolved. This statement defines the required evidence;
it does not assert whether those later events have already occurred.

## 11. Residual limitations

- The upstream 16.3.x `cacheComponents` / `use cache` memory report remains a watch item and is not
  evidenced as applicable to current OpenInvest.
- Local gates do not replace exact-head GitHub CI.
- Upgrading past the two named advisories does not prove absence of all Next.js vulnerabilities.
- Project-specific Windows/AVIF exploitability remains unevidenced.
- If the auxiliary runner is reused, its changed-file accounting must include untracked files; this is a tooling limitation, not an OpenInvest runtime defect.
- P3-06, P3-07, P3-08 and P3-10 remain out of scope.

## 12. Governance / next gates

P3-09 remains OPEN.

Completed publication gates:

1. implementation commit and push — `5ea1f7f29cf0ab9225460e01076255f32e2cf4cf`;
2. Draft PR #101 opened against `develop`;
3. exact-head GitHub CI #289 / run `33274458971` — `10/10 SUCCESS`;
4. fresh External published-head review attempt 1 — `REQUEST CHANGES` with
   `EXT-STAGE-03-41-P3-01`, documentation/governance evidence integrity only.

This revision is the docs-only remediation for `EXT-STAGE-03-41-P3-01`. It does not change
`frontend-next/package.json` or `frontend-next/pnpm-lock.yaml`.

Post-remediation progression requirements:

1. the docs-only remediation must be published on an exact PR head;
2. required GitHub CI must pass on that revised exact head;
3. a fresh exact-published-head External re-review must determine whether
   `EXT-STAGE-03-41-P3-01` is resolved;
4. after an External `APPROVED` verdict, the withheld Internal review chronology must be published
   in an evidence-only follow-up;
5. that evidence-only head must receive required CI and exact verification showing no
   runtime/dependency semantic change;
6. squash merge still requires explicit human authorization;
7. P3-09 can become CLOSED only through the separate closure-governance activation.

These are workflow requirements, not assertions that any particular later gate is currently pending
or incomplete. Their completion state must be established from the corresponding exact-head GitHub
and review evidence.

No statement in this dossier authorizes Ready, merge, branch deletion, or protected-branch mutation.
