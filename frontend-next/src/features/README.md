# Web presentation features

Feature folders may compose routes, view models, and UI against the Go OpenAPI contract in later
approved stages. They must not contain portfolio, financial, tax, or external-provider logic.
